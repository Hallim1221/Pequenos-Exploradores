<!DOCTYPE html>
<html lang="pt-br">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Turmas - <%= serie %></title>
  <link href="https://fonts.googleapis.com/css2?family=Baloo+2:wght@700;800&family=Fredoka:wght@400;700&display=swap" rel="stylesheet">
  <style>
    body { background: linear-gradient(120deg, #3bb273 0%, #00CFFF 100%); font-family: 'Fredoka', 'Baloo 2', Arial, sans-serif; min-height: 100vh; margin: 0; }
    .container { max-width: 420px; margin: 4em auto; background: #fff; border-radius: 18px; box-shadow: 0 4px 32px 0 rgba(60,180,115,0.10); padding: 2.5em 2em; }
    h1 { color: #00CFFF; font-family: 'Baloo 2', cursive; text-align: center; font-size: 2em; }
    .turmas-list { display: flex; flex-direction: column; gap: 1.2em; margin-top: 2em; }
    .turma-btn { background: linear-gradient(90deg, #FFD600 0%, #00CFFF 100%); color: #fff; font-weight: 800; font-size: 1.15em; border: none; border-radius: 10px; padding: 1em; cursor: pointer; transition: background 0.18s, transform 0.18s; }
    .turma-btn:hover { background: linear-gradient(90deg, #00CFFF 0%, #FFD600 100%); transform: scale(1.04); }
    .voltar { margin-top: 2em; display: block; text-align: center; color: #00CFFF; text-decoration: underline; font-weight: 700; }
  </style>
</head>
<body>
  <div class="container" style="position:relative; overflow:hidden;">
    <svg style="position:absolute;top:-40px;right:-40px;z-index:0;opacity:0.13;" width="180" height="180"><circle cx="90" cy="90" r="90" fill="#FFD600"/></svg>
    <svg style="position:absolute;bottom:-40px;left:-40px;z-index:0;opacity:0.13;" width="180" height="180"><circle cx="90" cy="90" r="90" fill="#00CFFF"/></svg>
    <h1 style="z-index:1;position:relative;">
      <span style="font-size:1.5em;vertical-align:middle;">🏫</span> Turmas do <span style="color:#FFD600"><%= serie %></span>
    </h1>
    <div class="turmas-list" style="z-index:1;position:relative;">
      <% turmas.forEach(function(turma, idx) { %>
        <form action="/professor/serie/<%= serie %>/turma/<%= turma %>" method="get" style="margin:0;">
          <button class="turma-btn" type="submit" style="display:flex;align-items:center;gap:0.7em;box-shadow:0 2px 12px 0 rgba(0,207,255,0.09);animation:fadeInTurma 0.5s <%= idx*0.07 %>s both;">
            <span style="font-size:1.5em;">👥</span> Turma <%= turma %>
          </button>
        </form>
      <% }) %>
    </div>
    <a href="/professor/dashboard" class="voltar" style="z-index:1;position:relative;">⏪ Voltar para séries</a>
  </div>
  <style>
    @keyframes fadeInTurma {
      from { opacity:0; transform:translateY(30px) scale(0.95); }
      to { opacity:1; transform:translateY(0) scale(1); }
    }
    .turma-btn {
      background: linear-gradient(90deg, #FFD600 0%, #00CFFF 100%);
      color: #fff;
      font-weight: 800;
      font-size: 1.15em;
      border: none;
      border-radius: 14px;
      padding: 1.1em 1.5em;
      cursor: pointer;
      transition: background 0.18s, transform 0.18s, box-shadow 0.18s;
      margin-bottom: 0.2em;
      box-shadow: 0 2px 12px 0 rgba(0,207,255,0.09);
      outline: none;
    }
    .turma-btn:hover {
      background: linear-gradient(90deg, #00CFFF 0%, #FFD600 100%);
      transform: scale(1.06) rotate(-1deg);
      box-shadow: 0 6px 24px 0 rgba(0,207,255,0.18);
    }
    .voltar {
      margin-top: 2em;
      display: block;
      text-align: center;
      color: #00CFFF;
      text-decoration: underline;
      font-weight: 700;
      font-size: 1.1em;
      letter-spacing: 0.01em;
      transition: color 0.18s;
    }
    .voltar:hover { color: #FFD600; }
  </style>
</body>
</html>
