<style>
  .pagina-central {
    max-width: 900px;
    margin: 2.5em auto 0 auto;
    padding: 2.2em 1.5em 2.5em 1.5em;
    background: #fff;
    border-radius: 18px;
    box-shadow: 0 4px 32px 0 rgba(60, 180, 115, 0.10), 0 1.5px 8px 0 rgba(0,0,0,0.04);
    min-height: 60vh;
    position: relative;
    z-index: 1;
    display: flex;
    flex-direction: column;
    align-items: center;
  }
  .pagina-central h1 {
    text-align: center;
    font-family: 'Baloo 2', 'Fredoka', Arial, sans-serif;
    font-size: 2em;
    color: #00CFFF;
    font-weight: 800;
    margin-bottom: 1.2em;
    margin-top: 0.5em;
    letter-spacing: 0.01em;
  }
  .pagina-central p, .pagina-central ul, .pagina-central li {
    font-size: 1.13em;
    color: #222;
    font-family: 'Fredoka', 'Quicksand', Arial, sans-serif;
    line-height: 1.7;
    margin-bottom: 1.1em;
    text-align: center;
  }
  .pagina-central ul {
    padding-left: 1.2em;
    text-align: left;
  }
  @media (max-width: 900px) {
    .pagina-central {
      max-width: 98vw;
      padding: 1.2em 0.2em 2em 0.2em;
    }
  }
  @media (max-width: 600px) {
    .pagina-central {
      margin: 1.2em auto 0 auto;
      padding: 0.7em 0.1em 1.2em 0.1em;
      border-radius: 10px;
    }
    .pagina-central h1 {
      font-size: 1.25em;
    }
  }
</style>
<!-- View quiz_meioambiente -->
<meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1, user-scalable=no">
<link href="https://fonts.googleapis.com/css2?family=Baloo+2:wght@700;800&display=swap" rel="stylesheet">
<button class="btn-voltar" onclick="window.history.back()" aria-label="Voltar" style="z-index: 11000; position: fixed; top: 0.5em; right: 0.5em; left: auto;">
  <svg width="48" height="48" viewBox="0 0 48 48" fill="none" xmlns="http://www.w3.org/2000/svg" aria-hidden="true">
    <circle cx="24" cy="24" r="22" fill="#00CFFF"/>
    <polygon points="30,14 18,24 30,34" fill="#0077B6"/>
    <ellipse cx="38" cy="38" rx="2.5" ry="1.5" fill="#B2F7EF"/>
  </svg>
</button>
<div class="top-banner"></div>

<!-- Sidebar Menu -->
<nav class="sidebar-menu" id="sidebarMenu">
  <button class="sidebar-close" onclick="document.getElementById('sidebarMenu').classList.remove('open')">&times;</button>
  <ul>
    <li><a href="/">Início</a></li>
    <li><a href="/sobre">Sobre</a></li>
    <li><a href="/contato">Contato</a></li>
    <li><a href="/loja">Loja</a></li>
  </ul>
</nav>
<button class="sidebar-toggle" onclick="document.getElementById('sidebarMenu').classList.add('open')" aria-label="Abrir menu lateral" style="background: transparent; border: none; box-shadow: none;">
  <svg width="28" height="28" viewBox="0 0 28 28"><rect y="4" width="28" height="4" rx="2" fill="#fff"/><rect y="12" width="28" height="4" rx="2" fill="#fff"/><rect y="20" width="28" height="4" rx="2" fill="#fff"/></svg>
</button>
<link rel="stylesheet" href="/css/btn-voltar.css">
<style>
  /* Sidebar Menu Styles */
  .sidebar-menu {
    position: fixed;
    top: 0;
    left: 0;
    width: 250px;
    height: 100vh;
    background: #3bb273;
    box-shadow: 2px 0 16px rgba(0,0,0,0.10);
    z-index: 12000;
    transform: translateX(-100%);
    transition: transform 0.3s cubic-bezier(.4,1.3,.5,1);
    padding-top: 2.5em;
    display: flex;
    flex-direction: column;
    gap: 1em;
  }
  .sidebar-menu.open {
    transform: translateX(0);
  }
  .sidebar-menu ul {
    list-style: none;
    padding: 0 1.2em;
    margin: 0;
    display: flex;
    flex-direction: column;
    gap: 1.2em;
  }
  .sidebar-menu li a {
    color: #fff;
    font-size: 1.18em;
    font-weight: 700;
    text-decoration: none;
    padding: 0.4em 0.2em;
    border-radius: 8px;
    transition: background 0.18s;
    display: block;
  }
  .sidebar-menu li a:hover {
    background: rgba(255,255,255,0.13);
  }
  .sidebar-close {
    position: absolute;
    top: 0.7em;
    right: 1em;
    background: none;
    border: none;
    font-size: 2em;
    color: #fff;
    cursor: pointer;
    z-index: 1;
    line-height: 1;
    transition: color 0.18s;
  }
  .sidebar-close:hover {
    color: #FFD600;
  }
  .sidebar-toggle {
    position: fixed;
    top: 0.7em;
    left: 0.7em;
    background: transparent;
    border: none;
    border-radius: 8px;
    padding: 0.3em 0.4em;
    z-index: 11500;
    cursor: pointer;
    box-shadow: none;
    transition: background 0.18s, border 0.18s;
  }
  .sidebar-toggle:hover {
    background: rgba(255,255,255,0.12);
    border: none;
  }
  @media (max-width: 600px) {
    .sidebar-menu { width: 80vw; min-width: 0; }
  }
</style>
<style>
  .top-banner {
    width: 100vw;
    min-width: 100vw;
    height: 32px;
    background: #3bb273;
    display: flex;
    align-items: center;
    justify-content: center;
    box-shadow: 0 2px 12px rgba(0,0,0,0.08);
    position: fixed;
    top: 0;
    left: 0;
    z-index: 10000;
    border-radius: 0;
    font-family: 'Fredoka', 'Quicksand', Arial, sans-serif;
    font-weight: 700;
    letter-spacing: 0.04em;
    font-size: 1.15em;
    color: #fff;
    overflow: hidden;
    animation: bannerFadeIn 0.7s;
    pointer-events: none;
  }
  .btn-voltar {
    pointer-events: auto;
  }
  @keyframes bannerFadeIn {
    from { opacity: 0; transform: translateY(-20px); }
    to { opacity: 1; transform: translateY(0); }
  }
  html, body {
    max-width: 100vw;
    overflow-x: hidden;
    box-sizing: border-box;
    min-height: 100vh;
  }
  body {
    background: linear-gradient(120deg, #3bb273 0%, #FFD600 100%);
    background-attachment: fixed;
    min-height: 100vh;
    font-family: 'Fredoka', 'Baloo 2', Arial, sans-serif;
    margin: 0;
    padding: 0;
    overflow-x: hidden;
    position: relative;
  }
  .bg-figma {
    position: fixed;
    top: 0;
    left: 0;
    width: 100vw;
    height: 100vh;
    z-index: 0;
    background: url('/img/tcc.img/tela inicio.png') center center/cover no-repeat;
    opacity: 0.13;
    pointer-events: none;
    transition: opacity 0.4s;
  }
  .quiz-tema {
    max-width: 700px;
    margin: 4.5em auto 0 auto;
    padding: 2.5em 2em 2.5em 2em;
    background: rgba(255,255,255,0.93);
    border-radius: 18px;
    box-shadow: 0 4px 32px 0 rgba(60, 180, 115, 0.10), 0 1.5px 8px 0 rgba(0,0,0,0.04);
    position: relative;
    z-index: 2;
    display: flex;
    flex-direction: column;
    align-items: center;
    animation: fadeInUp 0.7s;
  }
  @keyframes fadeInUp {
    from { opacity: 0; transform: translateY(40px); }
    to { opacity: 1; transform: translateY(0); }
  }
  .quiz-tema h1 {
    text-align: center;
    font-family: 'Baloo 2', 'Fredoka', Arial, sans-serif;
    font-size: 2em;
    color: #3bb273;
    font-weight: 800;
    margin-bottom: 1.2em;
    margin-top: 0.5em;
    letter-spacing: 0.01em;
  }
  .quiz-tema-desc {
    font-size: 1.18em;
    color: #FFD600;
    font-family: 'Fredoka', Arial, sans-serif;
    line-height: 1.7;
    margin-bottom: 1.5em;
    text-align: center;
    font-weight: 600;
  }
  .quiz-form {
    width: 100%;
    display: flex;
    flex-direction: column;
    gap: 1.5em;
    align-items: center;
  }
  .quiz-pergunta-card {
    background: linear-gradient(120deg, #fffbe7 60%, #3bb273 100%);
    border-radius: 14px;
    box-shadow: 0 2px 8px rgba(0,0,0,0.07);
    padding: 1.2em 1em 1.2em 1em;
    width: 100%;
    max-width: 480px;
    transition: box-shadow 0.18s, transform 0.18s;
    border: 1.5px solid #3bb273;
    animation: fadeInUp 0.7s;
    margin-bottom: 0.5em;
  }
  .quiz-pergunta-card legend {
    font-size: 1.13em;
    font-weight: 700;
    color: #3bb273;
    margin-bottom: 0.7em;
    font-family: 'Baloo 2', Arial, sans-serif;
  }
  .quiz-pergunta-card label {
    display: block;
    font-size: 1.08em;
    color: #FFD600;
    margin-bottom: 0.4em;
    cursor: pointer;
    font-weight: 600;
    padding-left: 0.2em;
    transition: color 0.18s;
  }
  .quiz-pergunta-card input[type="radio"] {
    accent-color: #3bb273;
    margin-right: 0.5em;
    transform: scale(1.15);
  }
  .quiz-tema-aviso {
    color: #3bb273;
    font-size: 1.1em;
    font-weight: 700;
    text-align: center;
    margin-top: 1.5em;
    margin-bottom: 0.5em;
  }
  .quiz-resultado {
    background: #fffbe7;
    border-radius: 12px;
    padding: 1em 1.2em;
    margin: 1.2em 0 1em 0;
    box-shadow: 0 1px 4px rgba(0,0,0,0.04);
    width: 100%;
    max-width: 480px;
    text-align: center;
    color: #3bb273;
    font-weight: 700;
  }
  .btn-destaque {
    display: inline-block;
    background: linear-gradient(90deg, #3bb273 0%, #FFD600 100%);
    color: #fff;
    font-size: 1.1em;
    font-weight: 700;
    border: none;
    border-radius: 10px;
    padding: 0.7em 1.7em;
    cursor: pointer;
    box-shadow: 0 2px 8px rgba(0,0,0,0.10);
    transition: background 0.18s, transform 0.18s, box-shadow 0.18s;
    margin-top: 0.5em;
    outline: none;
    text-decoration: none;
    letter-spacing: 0.02em;
    position: relative;
    overflow: hidden;
    appearance: button;
    font-family: 'Baloo 2', 'Fredoka', Arial, sans-serif;
  }
  .btn-destaque:after {
    content: '';
    position: absolute;
    left: 0; top: 0; right: 0; bottom: 0;
    border-radius: 10px;
    box-shadow: 0 0 0 0 #3bb273;
    transition: box-shadow 0.18s;
    pointer-events: none;
  }
  .btn-destaque:focus {
    outline: 2px solid #3bb273;
    outline-offset: 2px;
  }
  .btn-destaque:hover {
    background: linear-gradient(90deg, #FFD600 0%, #3bb273 100%);
    transform: scale(1.07);
    box-shadow: 0 6px 24px 0 rgba(60, 180, 115, 0.18), 0 2px 8px 0 rgba(0,0,0,0.13);
    text-decoration: none;
  }
  .btn-destaque:active {
    filter: brightness(0.97);
    transform: scale(0.98);
  }
  @media (max-width: 700px) {
    .quiz-tema {
      max-width: 98vw;
      padding: 1.2em 0.5em 1.5em 0.5em;
      border-radius: 10px;
    }
    .quiz-tema h1 {
      font-size: 1.25em;
    }
    .quiz-pergunta-card {
      max-width: 98vw;
    }
  }
</style>
<div class="bg-figma"></div>
<div style="height: 32px;"></div>
<main class="quiz-tema">
  <h1 class="quiz-tema-titulo">♻️ Quiz: Meio Ambiente</h1>
  <div class="quiz-tema-desc">Aprenda sobre reciclagem, preservação e sustentabilidade!</div>
  <form class="quiz-form" id="quizMeioAmbienteForm">
    <div class="quiz-pergunta-card">
      <fieldset>
        <legend>1. O que podemos fazer para economizar água?</legend>
        <label><input type="radio" name="q1" value="a"> Deixar a torneira aberta</label>
        <label><input type="radio" name="q1" value="b"> Tomar banhos rápidos</label>
        <label><input type="radio" name="q1" value="c"> Lavar o carro todo dia</label>
      </fieldset>
    </div>
    <div class="quiz-pergunta-card">
      <fieldset>
        <legend>2. Qual material é reciclável?</legend>
        <label><input type="radio" name="q2" value="a"> Plástico</label>
        <label><input type="radio" name="q2" value="b"> Casca de banana</label>
        <label><input type="radio" name="q2" value="c"> Óleo usado</label>
      </fieldset>
    </div>
    <div class="quiz-pergunta-card">
      <fieldset>
        <legend>3. O que é importante para preservar as florestas?</legend>
        <label><input type="radio" name="q3" value="a"> Desmatar</label>
        <label><input type="radio" name="q3" value="b"> Plantar árvores</label>
        <label><input type="radio" name="q3" value="c"> Jogar lixo no chão</label>
      </fieldset>
    </div>
    <div class="quiz-pergunta-card">
      <fieldset>
        <legend>4. O que devemos fazer com pilhas usadas?</legend>
        <label><input type="radio" name="q4" value="a"> Jogar no lixo comum</label>
        <label><input type="radio" name="q4" value="b"> Levar a pontos de coleta</label>
        <label><input type="radio" name="q4" value="c"> Enterrar no quintal</label>
      </fieldset>
    </div>
    <div class="quiz-pergunta-card">
      <fieldset>
        <legend>5. Qual atitude ajuda a proteger os animais?</legend>
        <label><input type="radio" name="q5" value="a"> Poluir rios</label>
        <label><input type="radio" name="q5" value="b"> Respeitar a natureza</label>
        <label><input type="radio" name="q5" value="c"> Caçar animais</label>
      </fieldset>
    </div>
    <button type="submit" class="btn-destaque">Enviar respostas</button>
  </form>
  <div id="resultadoMeioAmbiente" class="quiz-resultado" style="display:none;"></div>
  <button id="btnVoltarMeioAmbiente" class="btn-voltar-quiz" style="display:none;" onclick="window.location.href='/quiz'">Voltar para Quizzes</button>
  <script>
  document.getElementById('quizMeioAmbienteForm').onsubmit = function(e) {
    e.preventDefault();
    const respostas = {
      q1: 'b',
      q2: 'a',
      q3: 'b',
      q4: 'b',
      q5: 'b'
    };
    let acertos = 0;
    let resultado = '<h3>Resultado:</h3><ul>';
    for (let i = 1; i <= 5; i++) {
      const user = document.querySelector('input[name="q'+i+'"]:checked');
      if (user) {
        if (user.value === respostas['q'+i]) {
          acertos++;
          resultado += `<li>Questão ${i}: <span style='color:green'>✔️ Correta</span></li>`;
        } else {
          resultado += `<li>Questão ${i}: <span style='color:red'>❌ Errada</span></li>`;
        }
      } else {
        resultado += `<li>Questão ${i}: <span style='color:orange'>⚠️ Não respondida</span></li>`;
      }
    }
    resultado += `</ul><p><strong>Total de acertos: ${acertos} de 5</strong></p>`;
    document.getElementById('resultadoMeioAmbiente').innerHTML = resultado;
    document.getElementById('resultadoMeioAmbiente').style.display = 'block';
    document.getElementById('btnVoltarMeioAmbiente').style.display = 'inline-block';
    document.getElementById('quizMeioAmbienteForm').style.display = 'none';
  };
  </script>
  <div class="quiz-tema-aviso">Ajude o planeta e ganhe prêmios verdes! 🌎</div>
</main>
<%- include('partials/footer') %>
