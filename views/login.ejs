<link rel="stylesheet" href="/css/login.css">

<meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1, user-scalable=no">
<link href="https://fonts.googleapis.com/css2?family=Baloo+2:wght@700;800&display=swap" rel="stylesheet">
<button class="btn-voltar" onclick="window.history.back()" aria-label="Voltar" style="z-index: 11000; position: fixed; top: 0.5em; right: 0.5em; left: auto;">
  <svg width="60" height="60" viewBox="0 0 60 60" fill="none" xmlns="http://www.w3.org/2000/svg" aria-hidden="true" style="filter: drop-shadow(0 2px 10px #00CFFF88);">
    <circle cx="30" cy="30" r="27" fill="#00CFFF"/>
    <path d="M38 30c0 1.2-1 2.2-2.2 2.2H26.5l3.2 3.2c0.8 0.8 0.8 2.2 0 3-0.8 0.8-2.2 0.8-3 0l-7-7c-0.8-0.8-0.8-2.2 0-3l7-7c0.8-0.8 2.2-0.8 3 0 0.8 0.8 0.8 2.2 0 3l-3.2 3.2h9.3c1.2 0 2.2 1 2.2 2.2z" fill="#00518C"/>
  </svg>
</button>
<section class="top-banner"></section>


<nav class="sidebar-menu" id="sidebarMenu">
  <section style="width:100%;display:flex;flex-direction:column;align-items:center;padding:0.7em 0 0.2em 0;">
    <section style="background:#fff;border-radius:50%;box-shadow:0 4px 24px #0002,0 0 0 4px #fff;display:flex;align-items:center;justify-content:center;width:142px;height:142px;position:relative;">
      <img src="/img/shared image.png" alt="Logo Pequenos Exploradores" 
        style="width:126px;height:126px;object-fit:contain;display:block;background:none;filter:drop-shadow(0 2px 12px #0001);"
        loading="lazy">
    </section>
    <section style="margin-top:0.3em;text-align:center;width:100%;"></section>
  </section>
  <button class="sidebar-close" onclick="document.getElementById('sidebarMenu').classList.remove('open')">&times;</button>
  <ul>
    <li><a href="/">Início</a></li>
    <li><a href="/sobre">Sobre</a></li>
    <li><a href="https://www.instagram.com/pequenos_.exploradores_?igsh=MWthZnZ6eG1xeDlxdw==" target="_blank" rel="noopener">Contato</a></li>
<<<<<<< HEAD
    <li><a href="/loja">Loja</a></li>
=======
    <li><a href="/loja">Futuro</a></li>
>>>>>>> d37a33b (commit inicial do projeto e ajustes visuais na página aluno2)
  </ul>
</nav>
<button class="sidebar-toggle" onclick="document.getElementById('sidebarMenu').classList.add('open')" aria-label="Abrir menu lateral" style="background: transparent; border: none; box-shadow: none; position: fixed; top: 0; left: 0.7em; z-index: 11500;">
  <svg width="28" height="28" viewBox="0 0 28 28"><rect y="4" width="28" height="4" rx="2" fill="#fff"/><rect y="12" width="28" height="4" rx="2" fill="#fff"/><rect y="20" width="28" height="4" rx="2" fill="#fff"/></svg>
</button>
<link rel="stylesheet" href="/css/btn-voltar.css">
<style>
  
  .sidebar-menu {
    position: fixed;
    top: 0;
    left: 0;
    width: 250px;
    height: 100vh;
    background: #3bb273;
    box-shadow: 2px 0 16px rgba(0,0,0,0.10);
    z-index: 12000;
    transform: translateX(-100%);
    transition: transform 0.3s cubic-bezier(.4,1.3,.5,1);
    padding-top: 2.5em;
    display: flex;
    flex-direction: column;
    gap: 1em;
  }
  .sidebar-menu.open {
    transform: translateX(0);
  }
  .sidebar-menu ul {
    list-style: none;
    padding: 0 1.2em;
    margin: 0;
    display: flex;
    flex-direction: column;
    gap: 1.2em;
  }
  .sidebar-menu li a {
    color: #fff;
    font-size: 1.18em;
    font-weight: 700;
    text-decoration: none;
    padding: 0.4em 0.2em;
    border-radius: 8px;
    transition: background 0.18s;
    display: block;
  }
  .sidebar-menu li a:hover {
    background: rgba(255,255,255,0.13);
  }
  .sidebar-close {
    position: absolute;
    top: 0.7em;
    right: 1em;
    background: none;
    border: none;
    font-size: 2em;
    color: #fff;
    cursor: pointer;
    z-index: 1;
    line-height: 1;
    transition: color 0.18s;
  }
  .sidebar-close:hover {
    color: #FFD600;
  }
  .sidebar-toggle {
    position: fixed;
    top: 0.7em;
    left: 0.7em;
    background: transparent;
    border: none;
    border-radius: 8px;
    padding: 0.3em 0.4em;
    z-index: 11500;
    cursor: pointer;
    box-shadow: none;
    transition: background 0.18s, border 0.18s;
  }
  .sidebar-toggle:hover {
    background: rgba(255,255,255,0.12);
    border: none;
  }
  @media (max-width: 600px) {
    .sidebar-menu { width: 80vw; min-width: 0; }
  }
</style>
<style>
  .top-banner {
    width: 100vw;
    min-width: 100vw;
    height: 32px;
    background: #3bb273;
    display: flex;
    align-items: center;
    justify-content: center;
    box-shadow: 0 2px 12px rgba(0,0,0,0.08);
    position: fixed;
    top: 0;
    left: 0;
    z-index: 10000;
    border-radius: 0;
    font-family: 'Fredoka', 'Quicksand', Arial, sans-serif;
    font-weight: 700;
    letter-spacing: 0.04em;
    font-size: 1.15em;
    color: #fff;
    overflow: hidden;
    animation: bannerFadeIn 0.7s;
    pointer-events: none;
  }
  .btn-voltar {
    pointer-events: auto;
  }
  @keyframes bannerFadeIn {
    from { opacity: 0; transform: translateY(-20px); }
    to { opacity: 1; transform: translateY(0); }
  }
  html, body {
    max-width: 100vw;
    overflow-x: hidden;
    box-sizing: border-box;
    min-height: 100vh;
  }
  body {
    margin: 0;
    padding: 0;
    min-height: 100vh;
    font-family: 'Fredoka', 'Quicksand', Arial, sans-serif;
    position: relative;
  }
</style>
<section class="bg-figma"></section>
<section style="height: 32px;"></section>
<main class="login">
  <section class="bg-figma"></section>
  <section class="bg-darken"></section>
  <section class="pagina-central creative-login">
    <h1 class="login-title" style="color: #3bbcff; margin-top: 1.7em; text-shadow: 2px 2px 0 #000, -2px 2px 0 #000, 2px -2px 0 #000, -2px -2px 0 #000, 2px 0px 0 #000, 0px 2px 0 #000, -2px 0px 0 #000, 0px -2px 0 #000;">
      <span class="login-icon"></span> Bem-vindo ao mundo dos<br>Pequenos Exploradores!
    </h1>
    <section class="login-role-select creative-role-select role-select-box">
      <button type="button" class="role-btn selected" data-role="aluno">
        <span class="role-emoji">🎓</span>
        <span class="role-label">Aluno</span>
      </button>
      <button type="button" class="role-btn" data-role="professor">
        <span class="role-emoji">📚</span>
        <span class="role-label">Professor</span>
        
      </button>
    </section>

</main>
<style>
  .bg-darken {
    position: fixed;
    top: 0;
    left: 0;
    width: 100vw;
    height: 100vh;
    z-index: 0;
    background: rgba(0,0,0,0.45);
    pointer-events: none;
  }
  .pagina-central, .creative-login {
    background: none !important;
    box-shadow: none !important;
    backdrop-filter: none !important;
  }
  .bg-figma {
    position: fixed;
    top: 0;
    left: 0;
    width: 100vw;
    height: 100vh;
    z-index: 0;
    background: url('/img/tcc.img/tela inicio.png') center center/cover no-repeat;
    opacity: 0.32;
    pointer-events: none;
    transition: opacity 0.4s;
  }
  body { position: relative; }
  .creative-login {
    animation: fadeInUp 0.7s cubic-bezier(.23,1.01,.32,1) both;
    background: none;
    border-radius: 0;
    box-shadow: none;
    padding: 2.2em 1.5em 2.5em 1.5em;
    margin-top: 2.5em;
    max-width: 410px;
    margin-left: auto;
    margin-right: auto;
    position: relative;
    z-index: 1;
    backdrop-filter: none;
  }
  @keyframes fadeInUp {
    0% { opacity: 0; transform: translateY(40px); }
    100% { opacity: 1; transform: translateY(0); }
  }
  .login-title {
    font-size: 2em;
    font-weight: 800;
    /* color: #3bb273;  Removido para usar inline no h1 */
    text-align: center;
    margin-bottom: 1.1em;
    letter-spacing: 0.01em;
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 0.5em;
  }
  .login-icon {
    font-size: 1.5em;
    filter: drop-shadow(0 2px 6px #00cfff33);
  }
  .creative-role-select {
    display: flex;
    gap: 1.2em;
    justify-content: center;
    margin-bottom: 1.7em;
  }
  .role-btn {
    background: linear-gradient(120deg, #e6f7ee 60%, #f8e6ff 100%);
    color: #3bb273;
    border: 2.5px solid #e0e0e0;
    border-radius: 16px;
    padding: 1.1em 1.5em 1.1em 1.5em;
    font-size: 1.1em;
    font-weight: 700;
    cursor: pointer;
    box-shadow: 0 2px 12px 0 rgba(0,207,255,0.07);
    display: flex;
    flex-direction: column;
    align-items: center;
    min-width: 120px;
    transition: background 0.22s, border 0.22s, box-shadow 0.22s, color 0.22s;
    outline: none;
    position: relative;
  }
  .role-btn .role-emoji {
    font-size: 2.1em;
    margin-bottom: 0.1em;
    filter: drop-shadow(0 2px 6px #00cfff33);
  }
  .role-btn .role-label {
    font-size: 1.08em;
    font-weight: 800;
    margin-bottom: 0.1em;
  }
  .role-btn .role-desc {
    font-size: 0.93em;
    color: #7b2ff2;
    font-weight: 500;
    margin-top: 0.1em;
    opacity: 0.8;
  }
  .role-btn.selected, .role-btn:focus {
    background: linear-gradient(120deg, #00CFFF 0%, #3bb273 100%);
    color: #fff;
    border: 2.5px solid #3bb273;
    box-shadow: 0 4px 18px 0 rgba(0,207,255,0.13);
    transform: scale(1.04);
    z-index: 1;
  }
  .role-btn.selected .role-desc, .role-btn:focus .role-desc {
    color: #fff;
    opacity: 1;
  }
  .creative-form {
    width: 100%;
    max-width: 320px;
    margin: 0 auto;
    display: flex;
    flex-direction: column;
    gap: 1.1em;
    align-items: center;
    background: transparent;
    border-radius: 14px;
    box-shadow: none;
    padding: 1.2em 1.2em 1.5em 1.2em;
  }
  .creative-form label {
    font-weight: 700;
    color: #3bb273;
    align-self: flex-start;
    margin-bottom: 0.2em;
    margin-top: 0.2em;
    font-size: 1em;
  }
  .creative-form input[type="email"],
  .creative-form input[type="password"] {
    width: 100%;
    padding: 0.7em 1em;
    border-radius: 8px;
    border: 1.5px solid #e0e0e0;
    font-size: 1.05em;
    margin-bottom: 0.2em;
    background: #f7fafd;
    transition: border 0.18s, box-shadow 0.18s;
    box-shadow: 0 1px 4px 0 rgba(0,207,255,0.04);
  }
  .creative-form input[type="email"]:focus,
  .creative-form input[type="password"]:focus {
    border: 1.5px solid #3bb273;
    outline: none;
    box-shadow: 0 2px 8px 0 rgba(0,207,255,0.10);
  }
  .creative-btn {
    font-size: 1.1em;
    font-weight: 700;
    background: linear-gradient(90deg, #00CFFF 0%, #3bb273 100%);
    border: none;
    border-radius: 8px;
    padding: 0.6em 1.5em;
    color: #fff;
    box-shadow: 0 2px 8px 0 rgba(0,207,255,0.10);
    cursor: pointer;
    margin-top: 0.2em;
    transition: background 0.18s, box-shadow 0.18s;
  }
  .creative-btn:hover {
    background: linear-gradient(90deg, #3bb273 0%, #00CFFF 100%);
    box-shadow: 0 4px 16px 0 rgba(0,207,255,0.13);
  }
  .creative-back {
    margin-top: 0.7em;
    display: inline-block;
    font-size: 1.08em;
    font-weight: 800;
    background: linear-gradient(90deg, #FFD600 0%, #00CFFF 100%);
    color: #fff;
    border: none;
    border-radius: 8px;
    padding: 0.55em 1.5em;
    box-shadow: 0 2px 8px 0 rgba(0,207,255,0.10);
    cursor: pointer;
    text-decoration: none;
    letter-spacing: 0.01em;
    transition: background 0.18s, box-shadow 0.18s, color 0.18s, transform 0.18s;
    outline: none;
  }
  .creative-back:hover {
    background: linear-gradient(90deg, #00CFFF 0%, #FFD600 100%);
    color: #fffbe7;
    box-shadow: 0 4px 16px 0 rgba(0,207,255,0.18);
    transform: scale(1.04);
    text-decoration: none;
  }
  .creative-cta {
    margin-top: 1.5em;
    color: #3bb273;
    font-weight: 700;
    font-size: 1.08em;
    text-align: center;
    letter-spacing: 0.01em;
    background: rgba(230,247,238,0.45);
    border-radius: 10px;
    padding: 0.7em 1em;
    box-shadow: 0 1.5px 8px 0 rgba(0,207,255,0.04);
  }
  .cta-emoji {
    font-size: 1.3em;
    margin-right: 0.2em;
  }
</style>
<style>
  .role-select-box {
    background: linear-gradient(120deg, #0076CA 70%, #00CFFF 100%);
    border-radius: 22px;
    padding: 1.5em 1.2em 1.2em 1.2em;
    box-shadow: 0 2px 16px #0076CA33;
    margin-top: 7em;
    margin-bottom: 2em;
    display: flex;
    flex-direction: row;
    align-items: center;
    justify-content: center;
    gap: 1.2em;
    max-width: 370px;
    margin-left: auto;
    margin-right: auto;
  }
  /* Removido o flex-direction: column para manter os botões lado a lado em qualquer tamanho de tela */
  .role-select-box .role-btn {
    background: linear-gradient(120deg, #0076CA 70%, #00CFFF 100%);
    color: #fff;
    border: 2.5px solid #00CFFF;
    box-shadow: 0 2px 12px 0 rgba(0,207,255,0.13);
    transition: background 0.22s, border 0.22s, box-shadow 0.22s, color 0.22s;
  }
  .role-select-box .role-btn.selected, .role-select-box .role-btn:focus {
    background: linear-gradient(120deg, #0076CA 70%, #00CFFF 100%);
    color: #fff;
    border: 2.5px solid #00CFFF;
    box-shadow: 0 4px 18px 0 rgba(0,207,255,0.13);
    transform: scale(1.04);
    z-index: 1;
  }
  .role-select-box .role-btn .role-desc {
    color: #0076CA;
    opacity: 0.8;
  }
  .role-select-box .role-btn.selected .role-desc, .role-select-box .role-btn:focus .role-desc {
    color: #fff;
    opacity: 1;
  }
</style>
<script>
  
  document.addEventListener('DOMContentLoaded', function() {
    const roleBtns = document.querySelectorAll('.role-btn');
    const roleInput = document.getElementById('roleInput');
    const loginForm = document.getElementById('mainLoginForm');
    roleBtns.forEach(btn => {
      btn.addEventListener('click', function() {
        if (btn.getAttribute('data-role') === 'professor') {
          window.location.href = '/professor/login';
          return;
        }
        if (btn.getAttribute('data-role') === 'aluno') {
          window.location.href = '/aluno/login';
          return;
        }
        roleBtns.forEach(b => b.classList.remove('selected'));
        btn.classList.add('selected');
        roleInput.value = btn.getAttribute('data-role');
      });
      btn.addEventListener('keydown', function(e) {
        if (e.key === 'Enter' || e.key === ' ') {
          btn.click();
        }
      });
    });
    loginForm.addEventListener('submit', function(e) {
   
      const selectedBtn = document.querySelector('.role-btn.selected');
      if (selectedBtn) {
        roleInput.value = selectedBtn.getAttribute('data-role');
      }
    });
  });
</script>
<%- include('partials/footer') %>
