<style>
  .pagina-central {
    max-width: 420px;
    margin: 2.5em auto 0 auto;
    padding: 2.2em 1.5em 2.5em 1.5em;
    background: #fff;
    border-radius: 18px;
    box-shadow: 0 4px 32px 0 rgba(60, 180, 115, 0.10), 0 1.5px 8px 0 rgba(0,0,0,0.04);
    min-height: 40vh;
    position: relative;
    z-index: 1;
    display: flex;
    flex-direction: column;
    align-items: center;
  }
  .pagina-central h1 {
    text-align: center;
    font-family: 'Baloo 2', 'Fredoka', Arial, sans-serif;
    font-size: 2em;
    color: #3bb273;
    font-weight: 800;
    margin-bottom: 1.2em;
    margin-top: 0.5em;
    letter-spacing: 0.01em;
  }
  .pagina-central label, .pagina-central input {
    font-size: 1.13em;
    color: #222;
    font-family: 'Fredoka', 'Quicksand', Arial, sans-serif;
    line-height: 1.7;
    margin-bottom: 1.1em;
    text-align: center;
  }
  .pagina-central form {
    width: 100%;
    max-width: 320px;
    margin: 0 auto;
    display: flex;
    flex-direction: column;
    gap: 1.1em;
    align-items: center;
  }
  .pagina-central input {
    width: 100%;
    padding: 0.7em 1em;
    border-radius: 8px;
    border: 1.5px solid #3bb273;
    background: #f8fff9;
    font-size: 1em;
    font-family: 'Fredoka', 'Quicksand', Arial, sans-serif;
    margin-bottom: 0.7em;
    outline: none;
    transition: border 0.18s;
  }
  .pagina-central input:focus {
    border: 2px solid #00CFFF;
  }
  .pagina-central button[type="submit"] {
    background: linear-gradient(90deg, #00CFFF 0%, #3bb273 100%);
    color: #fff;
    font-weight: 800;
    font-size: 1.08em;
    border: none;
    border-radius: 8px;
    padding: 0.6em 1.5em;
    cursor: pointer;
    box-shadow: 0 2px 8px 0 rgba(0,207,255,0.10);
    transition: background 0.18s, box-shadow 0.18s, transform 0.18s;
    outline: none;
    margin-top: 0.2em;
  }
  .pagina-central button[type="submit"]:hover {
    background: linear-gradient(90deg, #3bb273 0%, #00CFFF 100%);
    box-shadow: 0 4px 16px 0 rgba(0,207,255,0.18);
    transform: scale(1.04);
  }
  @media (max-width: 600px) {
    .pagina-central {
      margin: 1.2em auto 0 auto;
      padding: 0.7em 0.1em 1.2em 0.1em;
      border-radius: 10px;
    }
    .pagina-central h1 {
      font-size: 1.25em;
    }
  }
</style>
<!-- View login -->
<meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1, user-scalable=no">
<link href="https://fonts.googleapis.com/css2?family=Baloo+2:wght@700;800&display=swap" rel="stylesheet">
<button class="btn-voltar" onclick="window.history.back()" aria-label="Voltar" style="z-index: 11000; position: fixed; top: 0.5em; right: 0.5em; left: auto;">
  <svg width="48" height="48" viewBox="0 0 48 48" fill="none" xmlns="http://www.w3.org/2000/svg" aria-hidden="true">
    <circle cx="24" cy="24" r="22" fill="#00CFFF"/>
    <polygon points="30,14 18,24 30,34" fill="#0077B6"/>
    <ellipse cx="38" cy="38" rx="2.5" ry="1.5" fill="#B2F7EF"/>
  </svg>
</button>
<div class="top-banner"></div>

<!-- Sidebar Menu -->
<nav class="sidebar-menu" id="sidebarMenu">
  <button class="sidebar-close" onclick="document.getElementById('sidebarMenu').classList.remove('open')">&times;</button>
  <ul>
    <li><a href="/">Início</a></li>
    <li><a href="/sobre">Sobre</a></li>
    <li><a href="/contato">Contato</a></li>
    <li><a href="/loja">Loja</a></li>
  </ul>
</nav>
<button class="sidebar-toggle" onclick="document.getElementById('sidebarMenu').classList.add('open')" aria-label="Abrir menu lateral" style="background: transparent; border: none; box-shadow: none;">
  <svg width="28" height="28" viewBox="0 0 28 28"><rect y="4" width="28" height="4" rx="2" fill="#fff"/><rect y="12" width="28" height="4" rx="2" fill="#fff"/><rect y="20" width="28" height="4" rx="2" fill="#fff"/></svg>
</button>
<link rel="stylesheet" href="/css/btn-voltar.css">
<style>
  /* Sidebar Menu Styles */
  .sidebar-menu {
    position: fixed;
    top: 0;
    left: 0;
    width: 250px;
    height: 100vh;
    background: #3bb273;
    box-shadow: 2px 0 16px rgba(0,0,0,0.10);
    z-index: 12000;
    transform: translateX(-100%);
    transition: transform 0.3s cubic-bezier(.4,1.3,.5,1);
    padding-top: 2.5em;
    display: flex;
    flex-direction: column;
    gap: 1em;
  }
  .sidebar-menu.open {
    transform: translateX(0);
  }
  .sidebar-menu ul {
    list-style: none;
    padding: 0 1.2em;
    margin: 0;
    display: flex;
    flex-direction: column;
    gap: 1.2em;
  }
  .sidebar-menu li a {
    color: #fff;
    font-size: 1.18em;
    font-weight: 700;
    text-decoration: none;
    padding: 0.4em 0.2em;
    border-radius: 8px;
    transition: background 0.18s;
    display: block;
  }
  .sidebar-menu li a:hover {
    background: rgba(255,255,255,0.13);
  }
  .sidebar-close {
    position: absolute;
    top: 0.7em;
    right: 1em;
    background: none;
    border: none;
    font-size: 2em;
    color: #fff;
    cursor: pointer;
    z-index: 1;
    line-height: 1;
    transition: color 0.18s;
  }
  .sidebar-close:hover {
    color: #FFD600;
  }
  .sidebar-toggle {
    position: fixed;
    top: 0.7em;
    left: 0.7em;
    background: transparent;
    border: none;
    border-radius: 8px;
    padding: 0.3em 0.4em;
    z-index: 11500;
    cursor: pointer;
    box-shadow: none;
    transition: background 0.18s, border 0.18s;
  }
  .sidebar-toggle:hover {
    background: rgba(255,255,255,0.12);
    border: none;
  }
  @media (max-width: 600px) {
    .sidebar-menu { width: 80vw; min-width: 0; }
  }
</style>
<style>
  .top-banner {
    width: 100vw;
    min-width: 100vw;
    height: 32px;
    background: #3bb273;
    display: flex;
    align-items: center;
    justify-content: center;
    box-shadow: 0 2px 12px rgba(0,0,0,0.08);
    position: fixed;
    top: 0;
    left: 0;
    z-index: 10000;
    border-radius: 0;
    font-family: 'Fredoka', 'Quicksand', Arial, sans-serif;
    font-weight: 700;
    letter-spacing: 0.04em;
    font-size: 1.15em;
    color: #fff;
    overflow: hidden;
    animation: bannerFadeIn 0.7s;
    pointer-events: none;
  }
  .btn-voltar {
    pointer-events: auto;
  }
  @keyframes bannerFadeIn {
    from { opacity: 0; transform: translateY(-20px); }
    to { opacity: 1; transform: translateY(0); }
  }
  html, body {
    max-width: 100vw;
    overflow-x: hidden;
    box-sizing: border-box;
    min-height: 100vh;
  }
  body {
    margin: 0;
    padding: 0;
    min-height: 100vh;
    font-family: 'Fredoka', 'Quicksand', Arial, sans-serif;
    position: relative;
  }
</style>
<div class="bg-figma"></div>
<div style="height: 32px;"></div>
<main class="login">
  <div class="bg-figma"></div>
  <div class="bg-darken"></div>
  <div class="pagina-central creative-login" style="background:none !important; box-shadow:none !important; backdrop-filter:none !important;">
    <h1 class="login-title">
      <span class="login-icon">🔐</span> Bem-vindo Explorador!
    </h1>
    <div class="login-role-select creative-role-select">
      <button type="button" class="role-btn selected" data-role="aluno">
        <span class="role-emoji">🎓</span>
        <span class="role-label">Sou Aluno</span>
        <span class="role-desc">Acesse sua jornada de aprendizado!</span>
      </button>
      <button type="button" class="role-btn" data-role="professor">
        <span class="role-emoji">📚</span>
        <span class="role-label">Sou Professor</span>
        <span class="role-desc">Gerencie e inspire exploradores!</span>
      </button>
    </div>
    <form method="post" action="/login" class="login-form creative-form" id="mainLoginForm">
      <input type="hidden" name="role" id="roleInput" value="aluno">
      <label for="email">Email</label>
      <input type="email" id="email" name="email" required placeholder="seu@email.com">
      <label for="senha">Senha</label>
      <input type="password" id="senha" name="senha" required placeholder="Digite sua senha">
      <button type="submit" class="btn-primary creative-btn">Entrar <span aria-label="Chave" role="img">🔑</span></button>
      <a href="/" class="btn-secondary creative-back">Voltar</a>
    </form>
    <div class="login-cta creative-cta">
      <span class="cta-emoji">🌱✨</span> Escolha seu perfil para acessar a aventura certa para você!
    </div>
  </div>
</main>
<style>
  .bg-darken {
    position: fixed;
    top: 0;
    left: 0;
    width: 100vw;
    height: 100vh;
    z-index: 0;
    background: rgba(0,0,0,0.45);
    pointer-events: none;
  }
  .pagina-central, .creative-login {
    background: none !important;
    box-shadow: none !important;
    backdrop-filter: none !important;
  }
  .bg-figma {
    position: fixed;
    top: 0;
    left: 0;
    width: 100vw;
    height: 100vh;
    z-index: 0;
    background: url('/img/tcc.img/tela inicio.png') center center/cover no-repeat;
    opacity: 0.32;
    pointer-events: none;
    transition: opacity 0.4s;
  }
  body { position: relative; }
  .creative-login {
    animation: fadeInUp 0.7s cubic-bezier(.23,1.01,.32,1) both;
    background: none;
    border-radius: 0;
    box-shadow: none;
    padding: 2.2em 1.5em 2.5em 1.5em;
    margin-top: 2.5em;
    max-width: 410px;
    margin-left: auto;
    margin-right: auto;
    position: relative;
    z-index: 1;
    backdrop-filter: none;
  }
  @keyframes fadeInUp {
    0% { opacity: 0; transform: translateY(40px); }
    100% { opacity: 1; transform: translateY(0); }
  }
  .login-title {
    font-size: 2em;
    font-weight: 800;
    color: #3bb273;
    text-align: center;
    margin-bottom: 1.1em;
    letter-spacing: 0.01em;
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 0.5em;
  }
  .login-icon {
    font-size: 1.5em;
    filter: drop-shadow(0 2px 6px #00cfff33);
  }
  .creative-role-select {
    display: flex;
    gap: 1.2em;
    justify-content: center;
    margin-bottom: 1.7em;
  }
  .role-btn {
    background: linear-gradient(120deg, #e6f7ee 60%, #f8e6ff 100%);
    color: #3bb273;
    border: 2.5px solid #e0e0e0;
    border-radius: 16px;
    padding: 1.1em 1.5em 1.1em 1.5em;
    font-size: 1.1em;
    font-weight: 700;
    cursor: pointer;
    box-shadow: 0 2px 12px 0 rgba(0,207,255,0.07);
    display: flex;
    flex-direction: column;
    align-items: center;
    min-width: 120px;
    transition: background 0.22s, border 0.22s, box-shadow 0.22s, color 0.22s;
    outline: none;
    position: relative;
  }
  .role-btn .role-emoji {
    font-size: 2.1em;
    margin-bottom: 0.1em;
    filter: drop-shadow(0 2px 6px #00cfff33);
  }
  .role-btn .role-label {
    font-size: 1.08em;
    font-weight: 800;
    margin-bottom: 0.1em;
  }
  .role-btn .role-desc {
    font-size: 0.93em;
    color: #7b2ff2;
    font-weight: 500;
    margin-top: 0.1em;
    opacity: 0.8;
  }
  .role-btn.selected, .role-btn:focus {
    background: linear-gradient(120deg, #00CFFF 0%, #3bb273 100%);
    color: #fff;
    border: 2.5px solid #3bb273;
    box-shadow: 0 4px 18px 0 rgba(0,207,255,0.13);
    transform: scale(1.04);
    z-index: 1;
  }
  .role-btn.selected .role-desc, .role-btn:focus .role-desc {
    color: #fff;
    opacity: 1;
  }
  .creative-form {
    width: 100%;
    max-width: 320px;
    margin: 0 auto;
    display: flex;
    flex-direction: column;
    gap: 1.1em;
    align-items: center;
    background: transparent;
    border-radius: 14px;
    box-shadow: none;
    padding: 1.2em 1.2em 1.5em 1.2em;
  }
  .creative-form label {
    font-weight: 700;
    color: #3bb273;
    align-self: flex-start;
    margin-bottom: 0.2em;
    margin-top: 0.2em;
    font-size: 1em;
  }
  .creative-form input[type="email"],
  .creative-form input[type="password"] {
    width: 100%;
    padding: 0.7em 1em;
    border-radius: 8px;
    border: 1.5px solid #e0e0e0;
    font-size: 1.05em;
    margin-bottom: 0.2em;
    background: #f7fafd;
    transition: border 0.18s, box-shadow 0.18s;
    box-shadow: 0 1px 4px 0 rgba(0,207,255,0.04);
  }
  .creative-form input[type="email"]:focus,
  .creative-form input[type="password"]:focus {
    border: 1.5px solid #3bb273;
    outline: none;
    box-shadow: 0 2px 8px 0 rgba(0,207,255,0.10);
  }
  .creative-btn {
    font-size: 1.1em;
    font-weight: 700;
    background: linear-gradient(90deg, #00CFFF 0%, #3bb273 100%);
    border: none;
    border-radius: 8px;
    padding: 0.6em 1.5em;
    color: #fff;
    box-shadow: 0 2px 8px 0 rgba(0,207,255,0.10);
    cursor: pointer;
    margin-top: 0.2em;
    transition: background 0.18s, box-shadow 0.18s;
  }
  .creative-btn:hover {
    background: linear-gradient(90deg, #3bb273 0%, #00CFFF 100%);
    box-shadow: 0 4px 16px 0 rgba(0,207,255,0.13);
  }
  .creative-back {
    margin-top: 0.7em;
    display: inline-block;
    font-size: 1.08em;
    font-weight: 800;
    background: linear-gradient(90deg, #FFD600 0%, #00CFFF 100%);
    color: #fff;
    border: none;
    border-radius: 8px;
    padding: 0.55em 1.5em;
    box-shadow: 0 2px 8px 0 rgba(0,207,255,0.10);
    cursor: pointer;
    text-decoration: none;
    letter-spacing: 0.01em;
    transition: background 0.18s, box-shadow 0.18s, color 0.18s, transform 0.18s;
    outline: none;
  }
  .creative-back:hover {
    background: linear-gradient(90deg, #00CFFF 0%, #FFD600 100%);
    color: #fffbe7;
    box-shadow: 0 4px 16px 0 rgba(0,207,255,0.18);
    transform: scale(1.04);
    text-decoration: none;
  }
  .creative-cta {
    margin-top: 1.5em;
    color: #3bb273;
    font-weight: 700;
    font-size: 1.08em;
    text-align: center;
    letter-spacing: 0.01em;
    background: rgba(230,247,238,0.45);
    border-radius: 10px;
    padding: 0.7em 1em;
    box-shadow: 0 1.5px 8px 0 rgba(0,207,255,0.04);
  }
  .cta-emoji {
    font-size: 1.3em;
    margin-right: 0.2em;
  }
</style>
<script>
  // Troca seleção de perfil com animação e redireciona para login de professor
  document.addEventListener('DOMContentLoaded', function() {
    const roleBtns = document.querySelectorAll('.role-btn');
    const roleInput = document.getElementById('roleInput');
    const loginForm = document.getElementById('mainLoginForm');
    roleBtns.forEach(btn => {
      btn.addEventListener('click', function() {
        if (btn.getAttribute('data-role') === 'professor') {
          window.location.href = '/professor/login';
          return;
        }
        roleBtns.forEach(b => b.classList.remove('selected'));
        btn.classList.add('selected');
        roleInput.value = btn.getAttribute('data-role');
      });
      btn.addEventListener('keydown', function(e) {
        if (e.key === 'Enter' || e.key === ' ') {
          btn.click();
        }
      });
    });
    loginForm.addEventListener('submit', function(e) {
      // Garante que o valor do role está correto no submit
      const selectedBtn = document.querySelector('.role-btn.selected');
      if (selectedBtn) {
        roleInput.value = selectedBtn.getAttribute('data-role');
      }
    });
  });
</script>
<%- include('partials/footer') %>
