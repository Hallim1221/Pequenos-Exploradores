<!DOCTYPE html>
<html lang="pt-br">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Alunos - <%= serie %> Turma <%= turma %></title>
  <link href="https://fonts.googleapis.com/css2?family=Baloo+2:wght@700;800&family=Fredoka:wght@400;700&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="/css/professor_alunos.css">
</head>
<body>
  <section class="container">
    <svg class="svg-top-right" width="180" height="180"><circle cx="90" cy="90" r="90" fill="#00CFFF"/></svg>
    <svg class="svg-bottom-left" width="180" height="180"><circle cx="90" cy="90" r="90" fill="#FFD600"/></svg>
    <h1><span class="titulo-icone">👩‍🏫</span> <span class="titulo-serie"><%= serie %></span> - Turma <span class="titulo-turma"><%= turma %></span></h1>
    <table class="alunos-table">
      <tr><th>Aluno</th><th>Pontuação</th></tr>
      <% alunos.forEach(function(aluno, idx) { %>
        <tr style="animation:fadeInAluno 0.5s <%= idx*0.03 %>s both;">
          <td><span class="aluno-icone">🎒</span> <%= aluno.nome %></td>
          <td><span class="aluno-icone">⭐</span> <%= aluno.pontuacao %></td>
        </tr>
      <% }) %>
    </table>
    <a href="/professor/serie/<%= serie %>" class="voltar">⏪ Voltar para turmas</a>
  </section>
</body>
</html>
