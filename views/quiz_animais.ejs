<link rel="stylesheet" href="/css/quiz_animais.css">
<link rel="stylesheet" href="/css/premium-aluno.css">
<!-- View quiz_animais -->
<meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1, user-scalable=no">
<script>
  if (localStorage.getItem('premiumAluno') === 'true') {
    document.body.classList.add('premium-aluno');
  }
</script>
<script>
  (function(){
    function forcePremiumQuizStyles(){
      if(localStorage.getItem('premiumAluno') !== 'true') return;
      document.body.classList.add('premium-aluno');
      console.log('premium-aluno active: forcing quiz styles (animais)');
      var sels = ['.pagina-central','.quiz-card','.quiz-tema','.quiz-pergunta-card','.btn-destaque','.quiz-tema h1','.quiz-pergunta-card legend'];
      sels.forEach(function(s){
        document.querySelectorAll(s).forEach(function(el){
          try{
            el.style.setProperty('background','linear-gradient(120deg,#a78bfa 0%,#7c3aed 100%)','important');
            el.style.setProperty('color','#fff','important');
            el.style.setProperty('box-shadow','0 4px 32px 0 #a78bfa33','important');
          }catch(e){}
        });
      });
    }
    if(document.readyState==='loading') document.addEventListener('DOMContentLoaded', forcePremiumQuizStyles);
    else forcePremiumQuizStyles();
  })();
</script>
<script>
  (function(){
    function applyPremiumInlineQuiz(){
      if(localStorage.getItem('premiumAluno') !== 'true') return;
      document.body.classList.add('premium-aluno');
      var selector = '.pagina-central, .quiz-card, .quiz-tema, .quiz-pergunta-card, .btn-destaque';
      var els = document.querySelectorAll(selector);
      els.forEach(function(el){
        try{
          el.style.setProperty('background', 'linear-gradient(120deg, #a78bfa 0%, #7c3aed 100%)', 'important');
          el.style.setProperty('color', '#fff', 'important');
          el.style.setProperty('box-shadow', '0 4px 32px 0 #a78bfa33', 'important');
        }catch(e){}
      });
    }
    if(document.readyState === 'loading') document.addEventListener('DOMContentLoaded', applyPremiumInlineQuiz);
    else applyPremiumInlineQuiz();
  })();
</script>
<link href="https://fonts.googleapis.com/css2?family=Baloo+2:wght@700;800&display=swap" rel="stylesheet">
<section class="top-banner"></section>

<!-- Sidebar Menu -->
<nav class="sidebar-menu" id="sidebarMenu">
  <section style="width:100%;display:flex;flex-direction:column;align-items:center;padding:0.7em 0 0.2em 0;">
    <section style="background:#fff;border-radius:50%;box-shadow:0 4px 24px #0002,0 0 0 4px #fff;display:flex;align-items:center;justify-content:center;width:142px;height:142px;position:relative;">
      <img src="/img/shared image.png" alt="Logo Pequenos Exploradores" 
        style="width:126px;height:126px;object-fit:contain;display:block;background:none;filter:drop-shadow(0 2px 12px #0001);"
        loading="lazy">
    </section>
    <section style="margin-top:0.3em;text-align:center;width:100%;"></section>
  </section>
  <button class="sidebar-close" onclick="document.getElementById('sidebarMenu').classList.remove('open')">&times;</button>
  <ul>
    <li><a href="/">Início</a></li>
    <li><a href="/sobre">Sobre</a></li>
    <li><a href="/contato">Contato</a></li>
<<<<<<< HEAD
    <li><a href="/loja">Loja</a></li>
=======
    <li><a href="/loja">Futuro</a></li>
>>>>>>> d37a33b (commit inicial do projeto e ajustes visuais na página aluno2)
  </ul>
</nav>
<button class="sidebar-toggle" onclick="document.getElementById('sidebarMenu').classList.add('open')" aria-label="Abrir menu lateral" style="background: transparent; border: none; box-shadow: none; position: fixed; top: 0; left: 0.7em; z-index: 11500;">
  <svg width="28" height="28" viewBox="0 0 28 28"><rect y="4" width="28" height="4" rx="2" fill="#fff"/><rect y="12" width="28" height="4" rx="2" fill="#fff"/><rect y="20" width="28" height="4" rx="2" fill="#fff"/></svg>
</button>
<link rel="stylesheet" href="/css/btn-voltar.css">
<style>
  /* Sidebar Menu Styles */
  .sidebar-menu {
    position: fixed;
    top: 0;
    left: 0;
    width: 250px;
    height: 100vh;
    background: #3bb273;
    box-shadow: 2px 0 16px rgba(0,0,0,0.10);
    z-index: 12000;
    transform: translateX(-100%);
    transition: transform 0.3s cubic-bezier(.4,1.3,.5,1);
    padding-top: 2.5em;
    display: flex;
    flex-direction: column;
    gap: 1em;
  }
  .sidebar-menu.open {
    transform: translateX(0);
  }
  .sidebar-menu ul {
    list-style: none;
    padding: 0 1.2em;
    margin: 0;
    display: flex;
    flex-direction: column;
    gap: 1.2em;
  }
  .sidebar-menu li a {
    color: #fff;
    font-size: 1.18em;
    font-weight: 700;
    text-decoration: none;
    padding: 0.4em 0.2em;
    border-radius: 8px;
    transition: background 0.18s;
    display: block;
  }
  .sidebar-menu li a:hover {
    background: rgba(255,255,255,0.13);
  }
  .sidebar-close {
    position: absolute;
    top: 0.7em;
    right: 1em;
    background: none;
    border: none;
    font-size: 2em;
    color: #fff;
    cursor: pointer;
    z-index: 1;
    line-height: 1;
    transition: color 0.18s;
  }
  .sidebar-close:hover {
    color: #FFD600;
  }
  .sidebar-toggle {
    position: fixed;
    top: 0.7em;
    left: 0.7em;
    background: transparent;
    border: none;
    border-radius: 8px;
    padding: 0.3em 0.4em;
    z-index: 11500;
    cursor: pointer;
    box-shadow: none;
    transition: background 0.18s, border 0.18s;
  }
  .sidebar-toggle:hover {
    background: rgba(255,255,255,0.12);
    border: none;
  }
  @media (max-width: 600px) {
    .sidebar-menu { width: 80vw; min-width: 0; }
  }
</style>
<style>
  .top-banner {
    width: 100vw;
    min-width: 100vw;
    height: 32px;
    background: #3bb273;
    display: flex;
    align-items: center;
    justify-content: center;
    box-shadow: 0 2px 12px rgba(0,0,0,0.08);
    position: fixed;
    top: 0;
    left: 0;
    z-index: 10000;
    border-radius: 0;
    font-family: 'Fredoka', 'Quicksand', Arial, sans-serif;
    font-weight: 700;
    letter-spacing: 0.04em;
    font-size: 1.15em;
    color: #fff;
    overflow: hidden;
    animation: bannerFadeIn 0.7s;
    pointer-events: none;
  }
  .btn-voltar {
    pointer-events: auto;
  }
  @keyframes bannerFadeIn {
    from { opacity: 0; transform: translateY(-20px); }
    to { opacity: 1; transform: translateY(0); }
  }
  html, body {
    max-width: 100vw;
    overflow-x: hidden;
    box-sizing: border-box;
    min-height: 100vh;
  }
  body {
    background: linear-gradient(120deg, #00CFFF 0%, #3bb273 100%);
    background-attachment: fixed;
    min-height: 100vh;
    font-family: 'Fredoka', 'Baloo 2', Arial, sans-serif;
    margin: 0;
    padding: 0;
    overflow-x: hidden;
    position: relative;
  }
  .bg-figma {
    position: fixed;
    top: 0;
    left: 0;
    width: 100vw;
    height: 100vh;
    z-index: 0;
    background: url('/img/tcc.img/tela inicio.png') center center/cover no-repeat;
    opacity: 0.13;
    pointer-events: none;
    transition: opacity 0.4s;
  }
  .quiz-tema {
    max-width: 700px;
    margin: 4.5em auto 0 auto;
    padding: 2.5em 2em 2.5em 2em;
    background: rgba(255,255,255,0.93);
    border-radius: 18px;
    box-shadow: 0 4px 32px 0 rgba(0, 207, 255, 0.10), 0 1.5px 8px 0 rgba(0,0,0,0.04);
    position: relative;
    z-index: 2;
    display: flex;
    flex-direction: column;
    align-items: center;
    animation: fadeInUp 0.7s;
  }
  @keyframes fadeInUp {
    from { opacity: 0; transform: translateY(40px); }
    to { opacity: 1; transform: translateY(0); }
  }
  .quiz-tema h1 {
    text-align: center;
    font-family: 'Baloo 2', 'Fredoka', Arial, sans-serif;
    font-size: 2em;
    color: #00CFFF;
    font-weight: 800;
    margin-bottom: 1.2em;
    margin-top: 0.5em;
    letter-spacing: 0.01em;
  }
  .quiz-tema-desc {
    font-size: 1.18em;
    color: #3bb273;
    font-family: 'Fredoka', Arial, sans-serif;
    line-height: 1.7;
    margin-bottom: 1.5em;
    text-align: center;
    font-weight: 600;
  }
  .quiz-form {
    width: 100%;
    display: flex;
    flex-direction: column;
    gap: 1.5em;
    align-items: center;
  }
  .quiz-pergunta-card {
    background: linear-gradient(120deg, #fffbe7 60%, #FFD600 100%);
    border-radius: 14px;
    box-shadow: 0 2px 8px rgba(0,0,0,0.07);
    padding: 1.2em 1em 1.2em 1em;
    width: 100%;
    max-width: 480px;
    transition: box-shadow 0.18s, transform 0.18s;
    border: 1.5px solid #FFD600;
    animation: fadeInUp 0.7s;
    margin-bottom: 0.5em;
  }
  .quiz-pergunta-card legend {
    font-size: 1.13em;
    font-weight: 700;
    color: #00CFFF;
    margin-bottom: 0.7em;
    font-family: 'Baloo 2', Arial, sans-serif;
  }
  .quiz-pergunta-card label {
    display: block;
    font-size: 1.08em;
    color: #3bb273;
    margin-bottom: 0.4em;
    cursor: pointer;
    font-weight: 600;
    padding-left: 0.2em;
    transition: color 0.18s;
  }
  .quiz-pergunta-card input[type="radio"] {
    accent-color: #00CFFF;
    margin-right: 0.5em;
    transform: scale(1.15);
  }
  .quiz-tema-aviso {
    color: #FFD600;
    font-size: 1.1em;
    font-weight: 700;
    text-align: center;
    margin-top: 1.5em;
    margin-bottom: 0.5em;
  }
  .quiz-resultado {
    background: #fffbe7;
    border-radius: 12px;
    padding: 1em 1.2em;
    margin: 1.2em 0 1em 0;
    box-shadow: 0 1px 4px rgba(0,0,0,0.04);
    width: 100%;
    max-width: 480px;
    text-align: center;
    color: #3bb273;
    font-weight: 700;
  }
  .btn-destaque {
    display: inline-block;
    background: linear-gradient(90deg, #FFD600 0%, #3bb273 100%);
    color: #fff;
    font-size: 1.1em;
    font-weight: 700;
    border: none;
    border-radius: 10px;
    padding: 0.7em 1.7em;
    cursor: pointer;
    box-shadow: 0 2px 8px rgba(0,0,0,0.10);
    transition: background 0.18s, transform 0.18s, box-shadow 0.18s;
    margin-top: 0.5em;
    outline: none;
    text-decoration: none;
    letter-spacing: 0.02em;
    position: relative;
    overflow: hidden;
    appearance: button;
    font-family: 'Baloo 2', 'Fredoka', Arial, sans-serif;
  }
  .btn-destaque:after {
    content: '';
    position: absolute;
    left: 0; top: 0; right: 0; bottom: 0;
    border-radius: 10px;
    box-shadow: 0 0 0 0 #FFD600;
    transition: box-shadow 0.18s;
    pointer-events: none;
  }
  .btn-destaque:focus {
    outline: 2px solid #FFD600;
    outline-offset: 2px;
  }
  .btn-destaque:hover {
    background: linear-gradient(90deg, #3bb273 0%, #FFD600 100%);
    transform: scale(1.07);
    box-shadow: 0 6px 24px 0 rgba(255, 214, 0, 0.18), 0 2px 8px 0 rgba(0,0,0,0.13);
    text-decoration: none;
  }
  .btn-destaque:active {
    filter: brightness(0.97);
    transform: scale(0.98);
  }
  @media (max-width: 700px) {
    .quiz-tema {
      max-width: 98vw;
      padding: 1.2em 0.5em 1.5em 0.5em;
      border-radius: 10px;
    }
    .quiz-tema h1 {
      font-size: 1.25em;
    }
    .quiz-pergunta-card {
      max-width: 98vw;
    }
  }
</style>
<!-- View quiz_animais -->
<meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1, user-scalable=no">
<link href="https://fonts.googleapis.com/css2?family=Baloo+2:wght@700;800&display=swap" rel="stylesheet">
<button class="btn-voltar" onclick="window.history.back()" aria-label="Voltar" style="z-index: 11000; position: fixed; top: 0.5em; right: 0.5em; left: auto;">
  <svg width="60" height="60" viewBox="0 0 60 60" fill="none" xmlns="http://www.w3.org/2000/svg" aria-hidden="true" style="filter: drop-shadow(0 2px 10px #00CFFF88);">
    <circle cx="30" cy="30" r="27" fill="#00CFFF"/>
    <path d="M38 30c0 1.2-1 2.2-2.2 2.2H26.5l3.2 3.2c0.8 0.8 0.8 2.2 0 3-0.8 0.8-2.2 0.8-3 0l-7-7c-0.8-0.8-0.8-2.2 0-3l7-7c0.8-0.8 2.2-0.8 3 0 0.8 0.8 0.8 2.2 0 3l-3.2 3.2h9.3c1.2 0 2.2 1 2.2 2.2z" fill="#00518C"/>
  </svg>
  <svg width="48" height="48" viewBox="0 0 48 48" fill="none" xmlns="http://www.w3.org/2000/svg" aria-hidden="true">
    <circle cx="24" cy="24" r="22" fill="#00CFFF"/>
    <polygon points="30,14 18,24 30,34" fill="#0077B6"/>
    <ellipse cx="38" cy="38" rx="2.5" ry="1.5" fill="#B2F7EF"/>
  </svg>
</button>
<section class="top-banner"></section>

<!-- Sidebar Menu -->
<nav class="sidebar-menu" id="sidebarMenu">
  <button class="sidebar-close" onclick="document.getElementById('sidebarMenu').classList.remove('open')">&times;</button>
  <ul>
    <li><a href="/">Início</a></li>
    <li><a href="/sobre">Sobre</a></li>
    <li><a href="/contato">Contato</a></li>
    <li><a href="/loja">Loja</a></li>
  </ul>
</nav>

  <svg width="28" height="28" viewBox="0 0 28 28"><rect y="4" width="28" height="4" rx="2" fill="#fff"/><rect y="12" width="28" height="4" rx="2" fill="#fff"/><rect y="20" width="28" height="4" rx="2" fill="#fff"/></svg>
</button>
<link rel="stylesheet" href="/css/btn-voltar.css">
<style>
  /* Sidebar Menu Styles */
  .sidebar-menu {
    position: fixed;
    top: 0;
    left: 0;
    width: 250px;
    height: 100vh;
    background: #3bb273;
    box-shadow: 2px 0 16px rgba(0,0,0,0.10);
    z-index: 12000;
    transform: translateX(-100%);
    transition: transform 0.3s cubic-bezier(.4,1.3,.5,1);
    padding-top: 2.5em;
    display: flex;
    flex-direction: column;
    gap: 1em;
  }
  .sidebar-menu.open {
    transform: translateX(0);
  }
  .sidebar-menu ul {
    list-style: none;
    padding: 0 1.2em;
    margin: 0;
    display: flex;
    flex-direction: column;
    gap: 1.2em;
  }
  .sidebar-menu li a {
    color: #fff;
    font-size: 1.18em;
    font-weight: 700;
    text-decoration: none;
    padding: 0.4em 0.2em;
    border-radius: 8px;
    transition: background 0.18s;
    display: block;
  }
  .sidebar-menu li a:hover {
    background: rgba(255,255,255,0.13);
  }
  .sidebar-close {
    position: absolute;
    top: 0.7em;
    right: 1em;
    background: none;
    border: none;
    font-size: 2em;
    color: #fff;
    cursor: pointer;
    z-index: 1;
    line-height: 1;
    transition: color 0.18s;
  }
  .sidebar-close:hover {
    color: #FFD600;
  }
  .sidebar-toggle {
    position: fixed;
    top: 0.7em;
    left: 0.7em;
    background: transparent;
    border: none;
    border-radius: 8px;
    padding: 0.3em 0.4em;
    z-index: 11500;
    cursor: pointer;
    box-shadow: none;
    transition: background 0.18s, border 0.18s;
  }
  .sidebar-toggle:hover {
    background: rgba(255,255,255,0.12);
    border: none;
  }
  @media (max-width: 600px) {
    .sidebar-menu { width: 80vw; min-width: 0; }
  }
</style>
<style>
  .top-banner {
    width: 100vw;
    min-width: 100vw;
    height: 32px;
    background: #3bb273;
    display: flex;
    align-items: center;
    justify-content: center;
    box-shadow: 0 2px 12px rgba(0,0,0,0.08);
    position: fixed;
    top: 0;
    left: 0;
    z-index: 10000;
    border-radius: 0;
    font-family: 'Fredoka', 'Quicksand', Arial, sans-serif;
    font-weight: 700;
    letter-spacing: 0.04em;
    font-size: 1.15em;
    color: #fff;
    overflow: hidden;
    animation: bannerFadeIn 0.7s;
    pointer-events: none;
  }
  .btn-voltar {
    pointer-events: auto;
  }
  @keyframes bannerFadeIn {
    from { opacity: 0; transform: translateY(-20px); }
    to { opacity: 1; transform: translateY(0); }
  }
  html, body {
    max-width: 100vw;
    overflow-x: hidden;
    box-sizing: border-box;
    min-height: 100vh;
  }
  body {
    background: linear-gradient(120deg, #00CFFF 0%, #3bb273 100%);
    background-attachment: fixed;
    min-height: 100vh;
    font-family: 'Fredoka', 'Baloo 2', Arial, sans-serif;
    margin: 0;
    padding: 0;
    overflow-x: hidden;
    position: relative;
  }
  .bg-figma {
    position: fixed;
    top: 0;
    left: 0;
    width: 100vw;
    height: 100vh;
    z-index: 0;
    background: url('/img/tcc.img/tela inicio.png') center center/cover no-repeat;
    opacity: 0.13;
    pointer-events: none;
    transition: opacity 0.4s;
  }
  .quiz-tema {
    max-width: 700px;
    margin: 4.5em auto 0 auto;
    padding: 2.5em 2em 2.5em 2em;
    background: rgba(255,255,255,0.93);
    border-radius: 18px;
    box-shadow: 0 4px 32px 0 rgba(0, 207, 255, 0.10), 0 1.5px 8px 0 rgba(0,0,0,0.04);
    position: relative;
    z-index: 2;
    display: flex;
    flex-direction: column;
    align-items: center;
    animation: fadeInUp 0.7s;
  }
  @keyframes fadeInUp {
    from { opacity: 0; transform: translateY(40px); }
    to { opacity: 1; transform: translateY(0); }
  }
  .quiz-tema h1 {
    text-align: center;
    font-family: 'Baloo 2', 'Fredoka', Arial, sans-serif;
    font-size: 2em;
    color: #00CFFF;
    font-weight: 800;
    margin-bottom: 1.2em;
    margin-top: 0.5em;
    letter-spacing: 0.01em;
  }
  .quiz-tema-desc {
    font-size: 1.18em;
    color: #3bb273;
    font-family: 'Fredoka', Arial, sans-serif;
    line-height: 1.7;
    margin-bottom: 1.5em;
    text-align: center;
    font-weight: 600;
  }
  .quiz-form {
    width: 100%;
    display: flex;
    flex-direction: column;
    gap: 1.5em;
    align-items: center;
  }
  .quiz-pergunta-card {
    background: linear-gradient(120deg, #fffbe7 60%, #FFD600 100%);
    border-radius: 14px;
    box-shadow: 0 2px 8px rgba(0,0,0,0.07);
    padding: 1.2em 1em 1.2em 1em;
    width: 100%;
    max-width: 480px;
    transition: box-shadow 0.18s, transform 0.18s;
    border: 1.5px solid #FFD600;
    animation: fadeInUp 0.7s;
    margin-bottom: 0.5em;
  }
  .quiz-pergunta-card legend {
    font-size: 1.13em;
    font-weight: 700;
    color: #00CFFF;
    margin-bottom: 0.7em;
    font-family: 'Baloo 2', Arial, sans-serif;
  }
  .quiz-pergunta-card label {
    display: block;
    font-size: 1.08em;
    color: #3bb273;
    margin-bottom: 0.4em;
    cursor: pointer;
    font-weight: 600;
    padding-left: 0.2em;
    transition: color 0.18s;
  }
  .quiz-pergunta-card input[type="radio"] {
    accent-color: #00CFFF;
    margin-right: 0.5em;
    transform: scale(1.15);
  }
  .quiz-tema-aviso {
    color: #FFD600;
    font-size: 1.1em;
    font-weight: 700;
    text-align: center;
    margin-top: 1.5em;
    margin-bottom: 0.5em;
  }
  .quiz-resultado {
    background: #fffbe7;
    border-radius: 12px;
    padding: 1em 1.2em;
    margin: 1.2em 0 1em 0;
    box-shadow: 0 1px 4px rgba(0,0,0,0.04);
    width: 100%;
    max-width: 480px;
    text-align: center;
    color: #3bb273;
    font-weight: 700;
  }
  .btn-destaque {
    display: inline-block;
    background: linear-gradient(90deg, #FFD600 0%, #3bb273 100%);
    color: #fff;
    font-size: 1.1em;
    font-weight: 700;
    border: none;
    border-radius: 10px;
    padding: 0.7em 1.7em;
    cursor: pointer;
    box-shadow: 0 2px 8px rgba(0,0,0,0.10);
    transition: background 0.18s, transform 0.18s, box-shadow 0.18s;
    margin-top: 0.5em;
    outline: none;
    text-decoration: none;
    letter-spacing: 0.02em;
    position: relative;
    overflow: hidden;
    appearance: button;
    font-family: 'Baloo 2', 'Fredoka', Arial, sans-serif;
  }
  .btn-destaque:after {
    content: '';
    position: absolute;
    left: 0; top: 0; right: 0; bottom: 0;
    border-radius: 10px;
    box-shadow: 0 0 0 0 #FFD600;
    transition: box-shadow 0.18s;
    pointer-events: none;
  }
  .btn-destaque:focus {
    outline: 2px solid #FFD600;
    outline-offset: 2px;
  }
  .btn-destaque:hover {
    background: linear-gradient(90deg, #3bb273 0%, #FFD600 100%);
    transform: scale(1.07);
    box-shadow: 0 6px 24px 0 rgba(255, 214, 0, 0.18), 0 2px 8px 0 rgba(0,0,0,0.13);
    text-decoration: none;
  }
  .btn-destaque:active {
    filter: brightness(0.97);
    transform: scale(0.98);
  }
  @media (max-width: 700px) {
    .quiz-tema {
      max-width: 98vw;
      padding: 1.2em 0.5em 1.5em 0.5em;
      border-radius: 10px;
    }
    .quiz-tema h1 {
      font-size: 1.25em;
    }
    .quiz-pergunta-card {
      max-width: 98vw;
    }
  }
</style>
<section class="bg-figma"></section>
<section style="height: 32px;"></section>
<main class="quiz-tema">
  <h1 style="font-size: 1.45em; font-weight: 700; margin-bottom: 0.6em; line-height: 1.18; word-break: break-word; letter-spacing: 0.01em; max-width: 98vw; margin-left: auto; margin-right: auto;">
    Animais do<br>
    <span style="color: #FFD600; font-size: 1.15em; font-weight: 800; letter-spacing: 0.03em;">Brasil</span>
  </h1>
  <section class="quiz-tema-desc">Descubra curiosidades sobre a fauna brasileira e ganhe moedinhas!</section>
  <form class="quiz-form" id="quizAnimaisForm">
    <section class="quiz-pergunta-card">
      <fieldset>
        <legend>1. Qual desses animais é mamífero?</legend>
        <label><input type="radio" name="q1" value="a"> 🐍 Cobra</label>
        <label><input type="radio" name="q1" value="b"> 🦁 Leão</label>
        <label><input type="radio" name="q1" value="c"> 🦅 Águia</label>
      </fieldset>
    </section>
    <section class="quiz-pergunta-card">
      <fieldset>
        <legend>2. Qual animal vive na água e tem barbatanas?</legend>
        <label><input type="radio" name="q2" value="a"> 🐘 Elefante</label>
        <label><input type="radio" name="q2" value="b"> 🐟 Peixe</label>
        <label><input type="radio" name="q2" value="c"> 🦒 Girafa</label>
      </fieldset>
    </section>
    <section class="quiz-pergunta-card">
      <fieldset>
        <legend>3. Qual animal é conhecido por construir tocas?</legend>
        <label><input type="radio" name="q3" value="a"> 🦦 Lontra</label>
        <label><input type="radio" name="q3" value="b"> 🦜 Papagaio</label>
        <label><input type="radio" name="q3" value="c"> 🦋 Borboleta</label>
      </fieldset>
    </section>
    <section class="quiz-pergunta-card">
      <fieldset>
        <legend>4. Qual animal é famoso por sua velocidade?</legend>
        <label><input type="radio" name="q4" value="a"> 🦓 Zebra</label>
        <label><input type="radio" name="q4" value="b"> 🦌 Cervo</label>
        <label><input type="radio" name="q4" value="c"> 🐆 Guepardo</label>
      </fieldset>
    </section>
    <section class="quiz-pergunta-card">
      <fieldset>
        <legend>5. Qual animal tem tromba?</legend>
        <label><input type="radio" name="q5" value="a"> 🐘 Elefante</label>
        <label><input type="radio" name="q5" value="b"> 🦏 Rinoceronte</label>
        <label><input type="radio" name="q5" value="c"> 🦒 Girafa</label>
      </fieldset>
    </section>
    <button type="submit" class="btn-destaque">Enviar respostas</button>
  </form>
  <section id="resultadoAnimais" class="quiz-resultado" style="display:none;"></section>
<<<<<<< HEAD
  <button id="btnVoltarAnimais" class="btn-voltar-quiz" style="display:none;" onclick="window.location.href='/quiz'">Voltar para Quizzes</button>
=======
  <button id="btnVoltarAnimais" class="btn-voltar-quiz" style="display:none;" onclick="window.location.href='/aluno2'">Voltar para Quizzes</button>
>>>>>>> d37a33b (commit inicial do projeto e ajustes visuais na página aluno2)
  <script>
  document.getElementById('quizAnimaisForm').onsubmit = function(e) {
    e.preventDefault();
    const respostas = {
      q1: 'b',
      q2: 'b',
      q3: 'a',
      q4: 'c',
      q5: 'a'
    };
    let acertos = 0;
    let resultado = '<h3>Resultado:</h3><ul>';
    for (let i = 1; i <= 5; i++) {
      const user = document.querySelector('input[name="q'+i+'"]:checked');
      if (user) {
        if (user.value === respostas['q'+i]) {
          acertos++;
          resultado += `<li>Questão ${i}: <span style='color:green'>✔️ Correta</span></li>`;
        } else {
          resultado += `<li>Questão ${i}: <span style='color:red'>❌ Errada</span></li>`;
        }
      } else {
        resultado += `<li>Questão ${i}: <span style='color:orange'>⚠️ Não respondida</span></li>`;
      }
    }
    resultado += `</ul><p><strong>Total de acertos: ${acertos} de 5</strong></p>`;
    document.getElementById('resultadoAnimais').innerHTML = resultado;
    document.getElementById('resultadoAnimais').style.display = 'block';
    document.getElementById('btnVoltarAnimais').style.display = 'inline-block';
    document.getElementById('quizAnimaisForm').style.display = 'none';
  };
  </script>
  <section class="quiz-tema-aviso">Responda e descubra se você é um super explorador dos animais! 🦁🐒</section>

</main>
<%- include('partials/footer') %>
</main>
<%- include('partials/footer') %>
